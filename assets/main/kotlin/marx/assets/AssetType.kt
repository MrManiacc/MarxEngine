package marx.assets

/**
 * This class stores all of the assets of the given type. Assets should be registered and manipulated through the registry,
 * but this asset type is more low level and wraps around assets. It allows for direct access to assets allowing for reloading and
 * deletion of files etc. Each asset can
 */
class AssetType<T : Asset<T>> {

}